#!/bin/sh
# Workbench has a single integration branch. This protects against an agent
# accidentally committing a task branch in this checkout.
branch="$(git branch --show-current)"
if [ "$branch" != "main" ]; then
  echo "Workbench commits must be made on main; refusing commit on ${branch:-detached HEAD}." >&2
  exit 1
fi
